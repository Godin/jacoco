plugins {
    id 'java'
}

jar {
    manifest {
        attributes(
                'Agent-Class': 'org.example.agent.Agent',
                'Can-Redefine-Classes': 'true',
                'Can-Retransform-Classes': 'true',
        )
    }
}
